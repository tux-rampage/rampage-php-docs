.. _filesystem.LocalFilesystem:

LocalFilesystem
===============

The ``rampage\\filesystem\\LocalFilesystem`` implements the :ref:`filesystem.fsinterface` for
a local filesystem on disk. It encapsulates ``DirectoryIterator`` and ``SplFileInfo`` to provide this functionality.

.. todo:: Complete FS Docs
